apiVersion: v1
kind: Service
metadata:
  name: {{ include "novapay-chart.fullname" . }}
  labels:
    {{- include "novapay-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "novapay-chart.selectorLabels" . | nindent 4 }}
